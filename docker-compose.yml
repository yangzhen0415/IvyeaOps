services:
  app:
    build: .
    container_name: ivyea-ops
    restart: unless-stopped
    ports:
      - "${PORT:-8080}:80"
    volumes:
      - ivyea-ops-data:/app/data
    env_file:
      - .env
    environment:
      - IVYEA_OPS_DATA_DIR=/app/data

volumes:
  ivyea-ops-data:
    driver: local
