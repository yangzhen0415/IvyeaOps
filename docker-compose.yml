services:
  app:
    build: .
    container_name: ops-hub
    restart: unless-stopped
    ports:
      - "${PORT:-8080}:80"
    volumes:
      - ops-hub-data:/app/data
    env_file:
      - .env
    environment:
      - OPSHUB_DATA_DIR=/app/data

volumes:
  ops-hub-data:
    driver: local
